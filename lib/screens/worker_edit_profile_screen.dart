import 'package:flutter/material.dart';

import 'worker_professional_profile_screen.dart';

class WorkerEditProfileScreen extends StatelessWidget {
  static const routeName = '/worker-edit-profile';

  const WorkerEditProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const WorkerProfessionalProfileScreen();
  }
}
